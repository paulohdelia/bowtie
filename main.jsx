import React from 'react';
import ReactDOM from 'react-dom/client';
import BowTieApp from './index.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BowTieApp />
  </React.StrictMode>,
);
